# 生产环境变量模板
# 复制此文件到项目根目录为 .env.production 并填写实际值
# 注意: .env.production 文件应该被 .gitignore 忽略,不要提交到版本控制

# Node.js 环境
NODE_ENV=production

# Next.js 应用端口(默认 3000,应与 Nginx 配置一致)
PORT=3000

# 域名和 basePath(用于构建时)
# 注意: basePath 已在 next.config.ts 中配置,这里仅作参考
# NEXT_PUBLIC_BASE_PATH=/play_chords
# NEXT_PUBLIC_SITE_URL=https://rookiiie.top

# 如需要其他环境变量,在此添加
# 例如: API 密钥、数据库连接等
# NEXT_PUBLIC_API_KEY=your_api_key_here

